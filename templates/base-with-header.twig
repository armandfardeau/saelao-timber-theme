{% extends 'base.twig' %}

{% block before_content %}
    <div>
        <h1 class='home-presentation__text'>
            {% block header_content %}
            {{ home.get_field('home_presentation_title') }}
            <span>{{ home.get_field('home_presentation_subtitle') }}</span>
            {% endblock %}
        </h1>
        <a href="{{ home.get_field('donate_button_link') }}" class="donate-button mobile-donate-button">
            <span>I want to</span>
            <span class="donate-bigger">donate</span>
        </a>
    </div>
    <div class="header__border" style="background-image: url({{ home.get_field('header_background_cropper') }})"></div>
    <div class="page-with-header">
{% endblock %}

{% block after_content %}
    </div>
{% endblock %}
